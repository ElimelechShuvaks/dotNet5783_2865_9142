<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.Background>
        <ImageBrush ImageSource="image\mainWindow\mainbakeground.jpg" ></ImageBrush>
    </Window.Background>
    
    <Window.Resources>
        <Style x:Key="common style" TargetType="Control">
            <Setter Property="Foreground" Value="Yellow">
            </Setter>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Color="Black" Offset="0.2"/>
                        <GradientStop  Color="Blue" Offset="0.5"/>
                        <GradientStop Color="Orange" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Button style" TargetType="Button"  BasedOn="{StaticResource common style}"/>
        <Style x:Key="label style" TargetType="Label" BasedOn="{StaticResource common style}"/>

    </Window.Resources>

    <Grid>
        <Label Content="The center for wholesale vehicles" Style="{StaticResource label style}" Height="60" VerticalAlignment="Top" 
               HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="18" />
        <Button Content="Catalog"  Click="ShowCatalogButton_Click"  Width="200" Height="75"  Background="White" BorderThickness="1" Margin="0,0,500,200"/>
    </Grid>
    
</Window>
